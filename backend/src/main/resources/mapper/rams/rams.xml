<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="briefing.crawling.mapper.RamsMapper">
    <select id="getRamsList" resultType="Rams">
        SELECT ram_size, ram_type, current_price, register_date, create_date_time
        FROM ram_month
        <where>
            <if test="ramType != null and ramType != ''">
                AND ram_type = #{ramType}
            </if>
            <if test="ramSize != null and ramSize != ''">
                AND ram_size = #{ramSize}
            </if>
        </where>
        ORDER BY register_date desc
    </select>

    <insert id="insertRams">
        INSERT INTO ram_month
        (
         ram_type,
         ram_size,
         current_price,
         register_date
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.ramType},
            #{item.ramSize},
            #{item.currentPrice},
            #{item.registerDate}
            )
        </foreach>
    </insert>

    <select id="getLastDatetime" resultType="java.time.OffsetDateTime">
        SELECT MAX(create_date_time)
        FROM ram_month
    </select>
</mapper>